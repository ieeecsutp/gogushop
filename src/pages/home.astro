
---
interface Props {
  onNavigate?: string;
}

const { onNavigate } = Astro.props;

const slides = [
  {
    id: 1,
    image: 'https://images.unsplash.com/photo-1566477712363-3c75dd39b416?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&ixid=M3w3Nzg4Nzd8MHwxfHNlYXJjaHwxfHxrcG9wJTIwY29uY2VydCUyMHN0YWdlJTIwbGlnaHRzfGVufDF8fHx8MTc1OTgwODU2NXww&ixlib=rb-4.1.0&q=80&w=1080',
    title: 'Gogushop Music',
    subtitle: 'Tu destino para lo mejor de',
    highlight: 'K-Pop Corea del Sur',
    description: 'Los álbumes más exclusivos de K-Pop directo desde Corea del Sur. Descubre la música que está conquistando el mundo.',
    artist: 'Colección Premium'
  },
  {
    id: 2,
    image: 'https://images.unsplash.com/photo-1720887237257-3d1ad1a06c8a?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&ixid=M3w3Nzg4Nzd8MHwxfHNlYXJjaHwxfHxrb3JlYW4lMjBwb3AlMjBtdXNpYyUyMHZpbnlsJTIwcmVjb3Jkc3xlbnwxfHx8fDE3NTk4MDg1Njh8MA&ixlib=rb-4.1.0&q=80&w=1080',
    title: 'Colección Exclusiva',
    subtitle: 'Álbumes limitados de',
    highlight: 'Tus Grupos Favoritos',
    description: 'BTS, ENHYPEN, TXT, TWICE, IVE, LE SSERAFIM y más. Ediciones especiales que no encontrarás en ningún otro lugar.',
    artist: 'Ediciones Limitadas'
  },
  {
    id: 3,
    image: 'https://images.unsplash.com/photo-1512352036558-e6fb1f0c8340?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&ixid=M3w3Nzg4Nzd8MHwxfHNlYXJjaHwxfHxrcG9wJTIwbXVzaWMlMjBjb2xvcmZ1bCUyMHN0YWdlJTIwcGVyZm9ybWFuY2V8ZW58MXx8fHwxNzU5ODA4NTcxfDA&ixlib=rb-4.1.0&q=80&w=1080',
    title: 'Experiencia K-Pop',
    subtitle: 'Vive la música como',
    highlight: 'Nunca Antes',
    description: 'Photocards exclusivas, pósters firmados y mercancía oficial. La experiencia K-Pop completa en tu hogar.',
    artist: 'Mercancía Oficial'
  }
];
---

<section class="hero-section relative min-h-screen flex items-center justify-center overflow-hidden">
  <!-- Background Images with Overlay -->
  <div class="absolute inset-0">
    {slides.map((slide, index) => (
      <div
        class={`slide-bg absolute inset-0 transition-opacity duration-1000 ${index === 0 ? 'opacity-100' : 'opacity-0'}`}
        data-slide={index}
      >
        <img
          src={slide.image}
          alt={slide.title}
          class="w-full h-full object-cover"
        />
        <div class="absolute inset-0 bg-gradient-to-r from-black/70 via-black/50 to-black/30"></div>
        <div class="absolute inset-0 bg-gradient-to-t from-primary/30 via-transparent to-secondary/20"></div>
      </div>
    ))}
  </div>

  <!-- Navigation Arrows -->
  <button
    id="prevSlide"
    class="btn-nav absolute left-6 top-1/2 -translate-y-1/2 z-20 bg-white/20 border border-white/30 hover:bg-white/30 backdrop-blur-md rounded-lg p-3 transition-all"
  >
    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-white"><path d="m15 18-6-6 6-6"/></svg>
  </button>
  
  <button
    id="nextSlide"
    class="btn-nav absolute right-6 top-1/2 -translate-y-1/2 z-20 bg-white/20 border border-white/30 hover:bg-white/30 backdrop-blur-md rounded-lg p-3 transition-all"
  >
    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-white"><path d="m9 18 6-6-6-6"/></svg>
  </button>

  <!-- Main Content -->
  <div class="container mx-auto px-4 py-20 relative z-10">
    <div class="text-center space-y-8 hero-content-animate">
      <!-- Badge -->
      <div class="badge gradient-music text-white px-6 py-3 text-sm border-0 inline-flex items-center rounded-full">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="mr-2 music-pulse"><path d="M9 18V5l12-2v13"/><circle cx="6" cy="18" r="3"/><circle cx="18" cy="16" r="3"/></svg>
        Álbumes Auténticos de Corea
      </div>
      
      <!-- Main Title -->
      <div class="space-y-4">
        <h1 class="text-5xl md:text-7xl lg:text-8xl font-bold leading-tight">
          <span class="slide-title bg-gradient-to-r from-primary via-secondary to-accent bg-clip-text text-transparent block">
            {slides[0].title}
          </span>
        </h1>
        
        <h2 class="text-xl md:text-3xl lg:text-4xl text-white/90">
          <span class="slide-subtitle">{slides[0].subtitle}</span>
          <span class="slide-highlight block text-secondary font-bold mt-2 neon-glow">
            {slides[0].highlight}
          </span>
        </h2>
        
        <p class="slide-description text-lg md:text-xl text-white/80 max-w-3xl mx-auto leading-relaxed">
          {slides[0].description}
        </p>
      </div>

      <!-- Special Offer Card -->
      <div class="max-w-2xl mx-auto">
        <div class="bg-white/10 backdrop-blur-strong rounded-3xl p-8 border border-white/20 shadow-3xl neon-glow">
          <div class="text-center">
            <div class="flex items-center justify-center mb-4">
              <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-secondary mr-3 vinyl-spin"><circle cx="12" cy="12" r="10"/><circle cx="12" cy="12" r="3"/></svg>
              <p class="text-lg font-bold text-white">🎵 OFERTA ESPECIAL DE LANZAMIENTO</p>
            </div>
            
            <h3 class="text-2xl md:text-3xl font-bold text-white mb-4">
              ¡25% OFF en Álbumes + Photocards Gratis!
            </h3>
            
            <div class="flex justify-center space-x-6 mb-6">
              <div class="text-center">
                <div class="bg-primary text-white rounded-xl px-4 py-3 font-bold text-xl border border-white/20">23</div>
                <p class="text-xs text-white/70 mt-2">Horas</p>
              </div>
              <div class="text-center">
                <div class="bg-secondary text-white rounded-xl px-4 py-3 font-bold text-xl border border-white/20">45</div>
                <p class="text-xs text-white/70 mt-2">Min</p>
              </div>
              <div class="text-center">
                <div class="bg-accent text-white rounded-xl px-4 py-3 font-bold text-xl border border-white/20">12</div>
                <p class="text-xs text-white/70 mt-2">Seg</p>
              </div>
            </div>
            
            <p class="text-white/80 mb-4">
              Código: <span class="font-bold text-secondary bg-secondary/20 px-3 py-1 rounded-lg border border-secondary/30">KPOP25</span>
            </p>
            
            <div class="flex items-center justify-center space-x-4 text-sm text-white/90">
              <span class="font-bold text-primary">25% OFF</span>
              <span>+</span>
              <span class="font-bold text-secondary">Photocards</span>
              <span>+</span>
              <span class="font-bold text-accent">Envío Gratis</span>
            </div>
          </div>
        </div>
      </div>

      <!-- Action Buttons -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <button 
          class="btn-primary gradient-music text-white px-8 py-4 text-lg rounded-xl shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-105 border-0 inline-flex items-center"
        >
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="mr-2"><polygon points="6 3 20 12 6 21 6 3"/></svg>
          Comprar Álbumes con 25% OFF
        </button>
        
        <button 
          class="btn-secondary border-2 border-white/30 text-white hover:bg-white/20 backdrop-blur-md px-8 py-4 text-lg rounded-xl transition-all duration-300"
        >
          Ver Testimonios de Fans
        </button>
      </div>

      <!-- Audio Control -->
      <div class="flex items-center justify-center space-x-4">
        <button
          id="audioToggle"
          class="border border-white/30 text-white/80 hover:bg-white/20 backdrop-blur-md px-4 py-2 rounded-lg inline-flex items-center transition-all"
        >
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="mr-2 audio-icon"><polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"/><path d="M15.54 8.46a5 5 0 0 1 0 7.07"/><path d="M19.07 4.93a10 10 0 0 1 0 14.14"/></svg>
          <span class="audio-text">Preview Musical</span>
        </button>
        <p class="text-white/60 text-sm">
          Escucha un preview de "<span class="slide-artist">{slides[0].artist}</span>"
        </p>
      </div>

      <!-- Stats -->
      <div class="flex flex-wrap gap-8 justify-center pt-8">
        <div class="text-center">
          <div class="flex items-center justify-center space-x-1 mb-2">
            {[...Array(5)].map((_, i) => (
              <svg key={i} xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="currentColor" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-secondary music-pulse"><path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.3 1.5 4.05 3 5.5l7 7Z"/></svg>
            ))}
          </div>
          <p class="text-sm text-white/70">1000+ álbumes disponibles</p>
        </div>
        <div class="text-center">
          <div class="text-3xl font-bold text-primary">98%</div>
          <p class="text-sm text-white/70">Fans satisfechos</p>
        </div>
        <div class="text-center">
          <div class="text-3xl font-bold text-accent">15K+</div>
          <p class="text-sm text-white/70">Álbumes vendidos</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Slide Indicators -->
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2 z-20 flex space-x-3">
    {slides.map((_, index) => (
      <button
        class={`slide-indicator w-3 h-3 rounded-full transition-all duration-300 ${index === 0 ? 'bg-white scale-125' : 'bg-white/50 hover:bg-white/75'}`}
        data-index={index}
      />
    ))}
  </div>

  <!-- Decorative Elements -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute top-20 left-10 w-32 h-32 bg-primary/20 rounded-full blur-xl animate-pulse"></div>
    <div class="absolute top-40 right-20 w-48 h-48 bg-secondary/20 rounded-full blur-xl animate-pulse delay-700"></div>
    <div class="absolute bottom-32 left-1/4 w-40 h-40 bg-accent/20 rounded-full blur-xl animate-pulse delay-1000"></div>
  </div>
</section>

<script>
  // Slide data
  const slides = [
    {
      id: 1,
      title: 'Gogushop Music',
      subtitle: 'Tu destino para lo mejor de',
      highlight: 'K-Pop Corea del Sur',
      description: 'Los álbumes más exclusivos de K-Pop directo desde Corea del Sur. Descubre la música que está conquistando el mundo.',
      artist: 'Colección Premium'
    },
    {
      id: 2,
      title: 'Colección Exclusiva',
      subtitle: 'Álbumes limitados de',
      highlight: 'Tus Grupos Favoritos',
      description: 'BTS, ENHYPEN, TXT, TWICE, IVE, LE SSERAFIM y más. Ediciones especiales que no encontrarás en ningún otro lugar.',
      artist: 'Ediciones Limitadas'
    },
    {
      id: 3,
      title: 'Experiencia K-Pop',
      subtitle: 'Vive la música como',
      highlight: 'Nunca Antes',
      description: 'Photocards exclusivas, pósters firmados y mercancía oficial. La experiencia K-Pop completa en tu hogar.',
      artist: 'Mercancía Oficial'
    }
  ];

  let currentSlide = 0;
  let isAutoPlaying = true;
  let isAudioPlaying = false;
  let autoPlayInterval: number;

  // Elements
  const slideBackgrounds = document.querySelectorAll('.slide-bg');
  const slideTitle = document.querySelector('.slide-title');
  const slideSubtitle = document.querySelector('.slide-subtitle');
  const slideHighlight = document.querySelector('.slide-highlight');
  const slideDescription = document.querySelector('.slide-description');
  const slideArtist = document.querySelector('.slide-artist');
  const indicators = document.querySelectorAll('.slide-indicator');
  const prevBtn = document.getElementById('prevSlide');
  const nextBtn = document.getElementById('nextSlide');
  const audioToggle = document.getElementById('audioToggle');
  const audioIcon = audioToggle?.querySelector('.audio-icon');
  const audioText = audioToggle?.querySelector('.audio-text');

  // Update slide content
  function updateSlideContent() {
    const slide = slides[currentSlide];
    
    if (slideTitle) slideTitle.textContent = slide.title;
    if (slideSubtitle) slideSubtitle.textContent = slide.subtitle;
    if (slideHighlight) slideHighlight.textContent = slide.highlight;
    if (slideDescription) slideDescription.textContent = slide.description;
    if (slideArtist) slideArtist.textContent = slide.artist;

    // Update backgrounds
    slideBackgrounds.forEach((bg, index) => {
      if (index === currentSlide) {
        bg.classList.remove('opacity-0');
        bg.classList.add('opacity-100');
      } else {
        bg.classList.remove('opacity-100');
        bg.classList.add('opacity-0');
      }
    });

    // Update indicators
    indicators.forEach((indicator, index) => {
      if (index === currentSlide) {
        indicator.classList.add('bg-white', 'scale-125');
        indicator.classList.remove('bg-white/50');
      } else {
        indicator.classList.remove('bg-white', 'scale-125');
        indicator.classList.add('bg-white/50');
      }
    });
  }

  // Navigation functions
  function goToSlide(index: number) {
    currentSlide = index;
    updateSlideContent();
    resetAutoPlay();
  }

  function nextSlide() {
    currentSlide = (currentSlide + 1) % slides.length;
    updateSlideContent();
    resetAutoPlay();
  }

  function prevSlide() {
    currentSlide = (currentSlide - 1 + slides.length) % slides.length;
    updateSlideContent();
    resetAutoPlay();
  }

  function resetAutoPlay() {
    isAutoPlaying = false;
    clearInterval(autoPlayInterval);
    setTimeout(() => {
      isAutoPlaying = true;
      startAutoPlay();
    }, 3000);
  }

  function startAutoPlay() {
    if (autoPlayInterval) clearInterval(autoPlayInterval);
    autoPlayInterval = setInterval(() => {
      if (isAutoPlaying) {
        currentSlide = (currentSlide + 1) % slides.length;
        updateSlideContent();
      }
    }, 5000);
  }

  function toggleAudio() {
    isAudioPlaying = !isAudioPlaying;
    
    if (audioIcon && audioText) {
      if (isAudioPlaying) {
        audioIcon.innerHTML = '<polygon points="6 4 10 4 10 20 6 20 6 4"/><rect width="4" height="16" x="14" y="4"/>';
        audioText.textContent = 'Pausar Preview';
      } else {
        audioIcon.innerHTML = '<polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"/><path d="M15.54 8.46a5 5 0 0 1 0 7.07"/><path d="M19.07 4.93a10 10 0 0 1 0 14.14"/>';
        audioText.textContent = 'Preview Musical';
      }
    }
  }

  // Event listeners
  prevBtn?.addEventListener('click', prevSlide);
  nextBtn?.addEventListener('click', nextSlide);
  audioToggle?.addEventListener('click', toggleAudio);

  indicators.forEach((indicator, index) => {
    indicator.addEventListener('click', () => goToSlide(index));
  });

  // Start autoplay
  startAutoPlay();
</script>

<style>
  .gradient-music {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  }

  .backdrop-blur-strong {
    backdrop-filter: blur(20px);
  }

  .shadow-3xl {
    box-shadow: 0 35px 60px -15px rgba(0, 0, 0, 0.3);
  }

  .neon-glow {
    text-shadow: 0 0 10px currentColor, 0 0 20px currentColor, 0 0 30px currentColor;
  }

  .music-pulse {
    animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }

  .vinyl-spin {
    animation: spin 3s linear infinite;
  }

  .hero-content-animate {
    animation: fadeInUp 1s ease-out;
  }

  @keyframes pulse {
    0%, 100% {
      opacity: 1;
    }
    50% {
      opacity: 0.5;
    }
  }

  @keyframes spin {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .delay-700 {
    animation-delay: 700ms;
  }

  .delay-1000 {
    animation-delay: 1000ms;
  }

  /* Define custom colors - adjust these to match your theme */
  :root {
    --primary: #667eea;
    --secondary: #764ba2;
    --accent: #f093fb;
  }

  .bg-primary {
    background-color: var(--primary);
  }

  .bg-secondary {
    background-color: var(--secondary);
  }

  .bg-accent {
    background-color: var(--accent);
  }

  .text-primary {
    color: var(--primary);
  }

  .text-secondary {
    color: var(--secondary);
  }

  .text-accent {
    color: var(--accent);
  }

  .border-secondary\/30 {
    border-color: rgba(118, 75, 162, 0.3);
  }

  .bg-secondary\/20 {
    background-color: rgba(118, 75, 162, 0.2);
  }
</style>


